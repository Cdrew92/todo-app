# todo-app
 React Todo App


# Local development
Run npm start

# Deployment
run `npm run build`
run `cd build`
run `surge`
select the project directory and the public URL where you want to publish the app
